import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: 'rgb(40,205,197)',
    },
    secondary: {
      main: 'rgb(33 136 131)',
    },
    black: {
      main: 'rgb(33,33,33)',
    },
    typography: {
      main: 'white',
    },
  },
});

export default theme;
